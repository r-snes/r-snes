[package]
name = "product_order"
version = "0.1.0"
edition = "2024"

[dependencies]
product_order_derive = { version = "0.1.0", path = "product_order_derive" }

[dev-dependencies]
duplicate.workspace = true

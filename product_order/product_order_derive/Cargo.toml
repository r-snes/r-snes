[lib]
proc-macro = true

[package]
name = "product_order_derive"
version = "0.1.0"
edition = "2024"

[dependencies]
syn = { version = "2.0", features = ["full"]}
quote = "1.0"
proc-macro2 = "1.0"

[dev-dependencies]
runtime-macros = "^1.1.1"
product_order = { path = "../" }
